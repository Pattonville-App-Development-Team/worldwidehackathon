import { SidenavFilterPipe } from './sidenav-filter.pipe';

describe('SidenavFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SidenavFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
