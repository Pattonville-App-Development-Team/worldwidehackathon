import { SidenavFilterPipePipe } from './sidenav-filter-pipe.pipe';

describe('SidenavFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SidenavFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
